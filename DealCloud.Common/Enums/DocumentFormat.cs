namespace DealCloud.Common.Enums
{
    public enum DocumentFormat
    {
        // NOTE: all values added to enumeration should have valid file extention value after .ToString() method call (.pdf, .docx for example)

        Pdf = 1,

        Docx = 2
    }
}